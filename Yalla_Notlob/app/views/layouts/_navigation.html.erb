<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Yalla Notlob</a>
      <ul class="nav navbar-nav">
      <li><%= link_to 'Home', root_path %></li>
      <% if user_signed_in? %>

        <%@user = current_user%>

        <li><%= link_to 'Friends', users_path  %></li>
        <li><%= link_to 'Groups', user_groups_path(user_id:@user.id)  %></li>
        <li><%= link_to 'Orders', user_orders_path(user_id:@user.id) %></li>
      </ul>
        <%end%>

    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-right">



       <%= link_to '#', class: "btn btn-default" do %>
     <i class="glyphicon glyphicon-bell notification-icon"></i>
<% end %>


        <% if user_signed_in? %>
        <!--This block is only visible to signed in admins -->
       <li><strong>  <%= current_user.name %></strong><li>
        &nbsp; &nbsp;
       <li>  <img src="<%= current_user.avatar %>" height="50px" width="50px"/></li>
        &nbsp; &nbsp;
        

          <li><%= link_to 'Logout', destroy_user_session_path,method: :delete %></li>
          <li><%= %></li>
        <!-- The block ends here -->
        <% else %>
          <li><%= link_to 'Login', new_user_session_path  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
